name: Test Python
description: Test Python package

runs:
  using: "composite"
  steps:
    - name: Run pytest
      shell: bash
      working-directory: ${{ github.workspace }}
      run: |
        source ./venv/bin/activate
        pytest
