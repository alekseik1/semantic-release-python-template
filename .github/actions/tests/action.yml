name: Test Python
description: Test Python package

runs:
  using: "composite"
  steps:
    - name: Run pytest
      shell: bash
      run: |
        source ${github.workspace}/venv/bin/activate
        pytest
